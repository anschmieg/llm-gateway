diff --git a/src/discovery.ts b/src/discovery.ts
index 54f702cb..5ce65c1f 100644
--- a/src/discovery.ts
+++ b/src/discovery.ts
@@ -1 +1 @@
-import { ProviderConfig } from './registry'
+import { ProviderConfig } from './registry';
@@ -4,3 +4,3 @@ export interface ModelInfo {
-  id: string
-  providerId: string
-  isCode: boolean
+  id: string;
+  providerId: string;
+  isCode: boolean;
@@ -13,2 +13 @@ export async function discoverModels(
-
-  const allModels: ModelInfo[] = []
+  const allModels: ModelInfo[] = [];
@@ -17,2 +16,2 @@ export async function discoverModels(
-    const apiKey = env[entry.secretVar]
-    if (!apiKey) return
+    const apiKey = env[entry.secretVar];
+    if (!apiKey) return;
@@ -21,2 +20,2 @@ export async function discoverModels(
-      const controller = new AbortController()
-      setTimeout(() => controller.abort(), 3000)
+      const controller = new AbortController();
+      setTimeout(() => controller.abort(), 3000);
@@ -26,2 +25,2 @@ export async function discoverModels(
-        signal: controller.signal
-      })
+        signal: controller.signal,
+      });
@@ -29 +28 @@ export async function discoverModels(
-      if (!res.ok) throw new Error(`Status ${res.status}`)
+      if (!res.ok) throw new Error(`Status ${res.status}`);
@@ -31 +30 @@ export async function discoverModels(
-      const data = await res.json() as { data: { id: string }[] }
+      const data = (await res.json()) as { data: { id: string }[] };
@@ -34 +33 @@ export async function discoverModels(
-      data.data.forEach(m => {
+      data.data.forEach((m) => {
@@ -40 +39,3 @@ export async function discoverModels(
-          const inWhitelist = entry.whitelist.some(pattern => id.includes(pattern.toLowerCase()));
+          const inWhitelist = entry.whitelist.some((pattern) =>
+            id.includes(pattern.toLowerCase())
+          );
@@ -47 +48,3 @@ export async function discoverModels(
-          const inBlacklist = entry.blacklist.some(pattern => id.includes(pattern.toLowerCase()));
+          const inBlacklist = entry.blacklist.some((pattern) =>
+            id.includes(pattern.toLowerCase())
+          );
@@ -52 +55,4 @@ export async function discoverModels(
-        const isCode = id.includes('code') || id.includes('deepseek') || id.includes('copilot');
+        const isCode =
+          id.includes('code') ||
+          id.includes('deepseek') ||
+          id.includes('copilot');
@@ -57 +63 @@ export async function discoverModels(
-          isCode
+          isCode,
@@ -60 +66 @@ export async function discoverModels(
-      console.log(`✅ Discovered ${data.data.length} models from ${entry.id}`)
+      console.log(`✅ Discovered ${data.data.length} models from ${entry.id}`);
@@ -62 +68 @@ export async function discoverModels(
-      console.warn(`❌ Failed to discover ${entry.id}`)
+      console.warn(`❌ Failed to discover ${entry.id}`);
@@ -64 +70 @@ export async function discoverModels(
-  })
+  });
@@ -66,3 +72,3 @@ export async function discoverModels(
-  await Promise.all(promises)
-  return allModels
-}
\ No newline at end of file
+  await Promise.all(promises);
+  return allModels;
+}
diff --git a/src/index.ts b/src/index.ts
index ab153ba1..e86f2b2b 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -18 +17,0 @@ app.use('/*', async (c, next) => {
-
@@ -24 +23 @@ app.post('/v1/chat/completions', async (c) => {
-    const discoveryPromise = discoverModels(providers, c.env).then(res => {
+    const discoveryPromise = discoverModels(providers, c.env).then((res) => {
@@ -31 +30,5 @@ app.post('/v1/chat/completions', async (c) => {
-      try { await discoveryPromise; } catch (e) { console.error("Discovery Failed", e); }
+      try {
+        await discoveryPromise;
+      } catch (e) {
+        console.error('Discovery Failed', e);
+      }
@@ -43 +46,5 @@ app.post('/v1/chat/completions', async (c) => {
-  if (requestedModelId && requestedModelId !== 'default' && requestedModelId !== 'gpt-3.5-turbo') {
+  if (
+    requestedModelId &&
+    requestedModelId !== 'default' &&
+    requestedModelId !== 'gpt-3.5-turbo'
+  ) {
@@ -45 +52 @@ app.post('/v1/chat/completions', async (c) => {
-    targetModel = MODEL_CACHE.find(m => m.id === requestedModelId);
+    targetModel = MODEL_CACHE.find((m) => m.id === requestedModelId);
@@ -48 +55,3 @@ app.post('/v1/chat/completions', async (c) => {
-      console.log(`🎯 Routing to requested model: ${targetModel.id} (${targetModel.providerId})`);
+      console.log(
+        `🎯 Routing to requested model: ${targetModel.id} (${targetModel.providerId})`
+      );
@@ -50 +59,3 @@ app.post('/v1/chat/completions', async (c) => {
-      console.log(`⚠️ Requested model '${requestedModelId}' not found in pool. Falling back to Smart Router.`);
+      console.log(
+        `⚠️ Requested model '${requestedModelId}' not found in pool. Falling back to Smart Router.`
+      );
@@ -57 +68 @@ app.post('/v1/chat/completions', async (c) => {
-    const lastMsg = body.messages?.slice(-1)[0]?.content || "";
+    const lastMsg = body.messages?.slice(-1)[0]?.content || '';
@@ -62,2 +73,4 @@ app.post('/v1/chat/completions', async (c) => {
-      targetModel = MODEL_CACHE.find(m => m.isCode && m.id.toLowerCase().includes('deepseek'))
-        || MODEL_CACHE.find(m => m.isCode);
+      targetModel =
+        MODEL_CACHE.find(
+          (m) => m.isCode && m.id.toLowerCase().includes('deepseek')
+        ) || MODEL_CACHE.find((m) => m.isCode);
@@ -66,2 +79,3 @@ app.post('/v1/chat/completions', async (c) => {
-      targetModel = MODEL_CACHE.find(m => m.id.toLowerCase().includes('gpt-4'))
-        || MODEL_CACHE.find(m => m.id.toLowerCase().includes('70b'));
+      targetModel =
+        MODEL_CACHE.find((m) => m.id.toLowerCase().includes('gpt-4')) ||
+        MODEL_CACHE.find((m) => m.id.toLowerCase().includes('70b'));
@@ -72 +86,3 @@ app.post('/v1/chat/completions', async (c) => {
-    console.log(`🧠 Smart Routing: Intent=${isCode ? 'Code' : 'Chat'} -> ${targetModel?.id}`);
+    console.log(
+      `🧠 Smart Routing: Intent=${isCode ? 'Code' : 'Chat'} -> ${targetModel?.id}`
+    );
@@ -75,2 +91,2 @@ app.post('/v1/chat/completions', async (c) => {
-  if (!targetModel) return c.json({ error: "No models available in registry" }, 503);
-
+  if (!targetModel)
+    return c.json({ error: 'No models available in registry' }, 503);
@@ -79,2 +95,4 @@ app.post('/v1/chat/completions', async (c) => {
-  const providerConfig = providers.find(p => p.id === targetModel!.providerId);
-  if (!providerConfig) return c.json({ error: "Provider config missing" }, 500);
+  const providerConfig = providers.find(
+    (p) => p.id === targetModel!.providerId
+  );
+  if (!providerConfig) return c.json({ error: 'Provider config missing' }, 500);
@@ -93 +111 @@ app.post('/v1/chat/completions', async (c) => {
-        'Authorization': `Bearer ${apiKey}`
+        Authorization: `Bearer ${apiKey}`,
@@ -95,2 +113,2 @@ app.post('/v1/chat/completions', async (c) => {
-      // IMPORTANT: We inject the *actual* targetModel.id. 
-      // If we used smart routing, this overwrites "default". 
+      // IMPORTANT: We inject the *actual* targetModel.id.
+      // If we used smart routing, this overwrites "default".
@@ -99 +117 @@ app.post('/v1/chat/completions', async (c) => {
-      signal: controller.signal
+      signal: controller.signal,
@@ -106,7 +124,10 @@ app.post('/v1/chat/completions', async (c) => {
-      return c.json({
-        error: {
-          message: `Upstream (${providerConfig.id}) Error`,
-          details: errText,
-          status: response.status
-        }
-      }, response.status as any);
+      return c.json(
+        {
+          error: {
+            message: `Upstream (${providerConfig.id}) Error`,
+            details: errText,
+            status: response.status,
+          },
+        },
+        response.status as any
+      );
@@ -117 +138 @@ app.post('/v1/chat/completions', async (c) => {
-      headers: response.headers
+      headers: response.headers,
@@ -119 +139,0 @@ app.post('/v1/chat/completions', async (c) => {
-
@@ -122 +142,9 @@ app.post('/v1/chat/completions', async (c) => {
-    return c.json({ error: { message: isTimeout ? "Gateway Timeout" : "Connection Failed", details: e.message } }, isTimeout ? 504 : 502);
+    return c.json(
+      {
+        error: {
+          message: isTimeout ? 'Gateway Timeout' : 'Connection Failed',
+          details: e.message,
+        },
+      },
+      isTimeout ? 504 : 502
+    );
@@ -126 +154 @@ app.post('/v1/chat/completions', async (c) => {
-export default app;
\ No newline at end of file
+export default app;
diff --git a/src/registry.ts b/src/registry.ts
index cf99d29a..e0ceba2f 100644
--- a/src/registry.ts
+++ b/src/registry.ts
@@ -2,4 +2,4 @@ export interface ProviderConfig {
-  id: string
-  baseUrl: string
-  secretVar: string
-  type: "openai" | "azure" | "anthropic"
+  id: string;
+  baseUrl: string;
+  secretVar: string;
+  type: 'openai' | 'azure' | 'anthropic';
@@ -8,2 +8,2 @@ export interface ProviderConfig {
-  whitelist?: string[]
-  blacklist?: string[]
+  whitelist?: string[];
+  blacklist?: string[];
@@ -36,3 +36,3 @@ export const providers: ProviderConfig[] = [
-    whitelist: ["gpt-4", "o1", "claude"]
-  }
-] as const;
\ No newline at end of file
+    whitelist: ['gpt-4', 'o1', 'claude'],
+  },
+] as const;
